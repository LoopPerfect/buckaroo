# Generated by Buckaroo - https://buckaroo.pm
include_defs('//BUCKAROO_DEPS')

cxx_library(
  name = '{{name}}',
  header_namespace = '{{name}}',
  srcs = glob([
    '{{name}}/src/**/*.cpp',
  ]),
  headers = subdir_glob([ # private include files
    ('{{name}}/detail', '**/*.h'), # they are only accesible inside the library
    ('{{name}}/detail', '**/*.hpp'),
  ]),
  exported_headers = subdir_glob([ # public include files
    ('{{name}}/include', '**/*.h'), # those will be exported
    ('{{name}}/include', '**/*.hpp'), # and accessible via <quick/header.h>
  ]),
  deps = BUCKAROO_DEPS,
  visibility = ['PUBLIC']
)

cxx_binary(
  name = 'main',
  srcs = ['{{name}}/apps/main.cpp'],
  deps = [':{{name}}'],
  visibility = ['PUBLIC']
)